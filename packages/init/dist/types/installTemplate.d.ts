export declare function installTemplate({ name, targetPath, npmName }: {
    type: string;
    name: string;
    targetPath: string;
    npmName: string;
}, { force }: {
    force: boolean;
}): void;
